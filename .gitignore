**/*.tfstate
**/*.json
**/*.exe
# Local .terraform directories
**/.terraform/*
**/terraform/environments/test/.terraform/*
**/terraform/environments/test/terraform.tfvars
/home/dahsage3/eegrOrg/eegrOsPrac/azNotes/project3Files/p5sub/Project-Ensuring-Quality-Releases-/terraform/environments/test/terraform.tfvars

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run. Most
# .tfvars files are managed as part of configuration and so should be included in
# version control.
#
# example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# automatedtesting/postman/Test.environment1.json~
**/screeshots/*
azure-pipelines10-tf.yml
azure-pipelines02.yml~
azure-pipelines10-tf.yml~
azure-pipelines.yml
azure-pipelines.yml~
azure-pipelines.yaml
azure-pipelines.yaml~
azure-pipelines9.yaml
azure-pipelines9.yaml~
azure-pipelines6-tf.yaml
azure-pipelines6-tf.yaml~
azure-pipelines5-tf.yaml
azure-pipelines5-tf.yaml~
azure-pipelines2.yaml~
azure-pipelines2.yaml
azure-pipelines2.yml
azure-pipelines-1.yml
azure-pipelines3.yml
azure-pipelines3.yml~
azure-pipeline_bkup.yml
